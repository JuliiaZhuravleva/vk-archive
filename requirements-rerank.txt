# Пересортировка выдачи кросс-энкодером (search.py --rerank).
#
# Ставить только если она нужна: сам torch весит сотни мегабайт, а модель
# bge-reranker-v2-m3 качает ещё ~2,2 ГБ в ~/.cache при первом запуске. Поиск
# без этого работает — пересортировка поднимает MRR с 0.402 до 0.592, но
# добавляет ~4 с на запрос.
#
# Нижние границы поставлены по совместимости API (AutoTokenizer,
# AutoModelForSequenceClassification), а мерилось всё на torch 2.13.0
# и transformers 5.14.1. У transformers потолок до следующего мажора:
# API там ломают часто, шестёрку сначала проверить руками.

-r requirements.txt

torch>=2.2
transformers>=4.40,<6
