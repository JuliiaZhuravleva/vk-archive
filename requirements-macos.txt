# Анализ фотографий: OCR, распознавание сцены, счёт лиц — через системный
# фреймворк Vision. Работает только на macOS и только на своей машине:
# наружу не уходит ни одна картинка.
#
# Cocoa указан явно, хотя и приходит следом за Quartz: analyze_photos.py
# импортирует Foundation напрямую, а зависимость, от которой зависишь в коде,
# лучше объявлять, а не получать по цепочке.
#
# Проверено на pyobjc 12.2.1.

-r requirements.txt

pyobjc-framework-Vision~=12.2
pyobjc-framework-Quartz~=12.2
pyobjc-framework-Cocoa~=12.2
